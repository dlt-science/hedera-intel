from os import path
from transactions.settings import PROJECT_ROOT

FIGURES_PATH = path.join(PROJECT_ROOT, "figures")
DATA_PATH = path.join(PROJECT_ROOT, "data")
RESULTS_PATH = path.join(PROJECT_ROOT, "results")
RUNTIME_THRESHOLD = 0.9
